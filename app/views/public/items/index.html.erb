<main>
  <div class="container px-5 px-sm-0 justify-content-center">
    <div class="row mt-4">
      <%= render 'public/homes/sidebar', genres: @genres %>
      <div class="col-9 mt-4 d-flex flex-wrap">
        <% @items.each do |item| %>
        <div class="p-2">
          <%= link_to item_path(item.id) do %>
            <%= image_tag item.get_item_image(200,150) %><br>
            <p><%= item.name %></p>
            <p>¥<%= item.price %></p>
          <% end %>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</main>