<div class="container px-5 px-sm-0">
  <div class="row">
    <%= render 'sidebar', genres: @genres %>
    <div class="col-sm-8 top_frame">
      <%= image_tag('top_frame.png', class: "img-fluid") %>
      <p>ようこそ、ながのCAKEへ！<br>
      このサイトは、ケーキ販売のECサイトです。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要です。</p>
      <h5><strong>新着商品</strong></h5>
      <div class="mt-4 d-flex flex-wrap">
        <% @items.each do |item| %>
        <div class="p-2 card w-25">
          <%= link_to item_path(item.id) do %>
            <%= image_tag item.get_item_image(200,150) %><br>
            <p><%= item.name %></p>
            <p>¥<%= item.price %></p>
          <% end %>
        </div>
        <% end %>
      </div>
      <div class="text-right">
        <%= link_to "全ての商品を見る >", items_path %>
      </div>
    </div>
  </div>
</div>