<div class="container">
  <div class="row mt-4">
    <h3>ジャンル編集</h3>
  </div>
  <div class="row mt-4">
    <%= form_with model: @genre, url: admin_genre_path do |f| %>
      <p>ジャンル名　<%= f.text_field :name %>　<%= f.submit '変更を保存', class: "btn btn-success" %></p>
    <% end %>
  </div>
</div>